# SPDX-License-Identifier: MIT-0
---
# vars file for authentik_container_setup

authentik_container_setup_network_mode: "{{ authentik_container_setup_networks[0].name }}"
authentik_container_setup_worker_network_mode: "{{ authentik_container_setup_worker_networks[0].name }}"
authentik_container_setup_redis_network_mode: "{{ authentik_container_setup_redis_networks[0].name }}"
